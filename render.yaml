services:
  - type: web
    name: magicpin-bot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 10000
